<UserControl x:Class="NoMansGUI.Views.MBinView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:diag="clr-namespace:System.Diagnostics;assembly=WindowsBase"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NoMansGUI.Views"
             xmlns:converters="clr-namespace:NoMansGUI.Utils.Converters"
             xmlns:templateselector="clr-namespace:NoMansGUI.Utils.TemplateSelectors">
    <UserControl.Resources>
        <templateselector:MBINTemplateSelector x:Key="MbinTemplateSelector"/>
    </UserControl.Resources>
    <Grid>
        <ScrollViewer x:Name="ScrollView"  VerticalScrollBarVisibility="Auto" CanContentScroll="True" MinHeight="400" PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
            <ListView HorizontalAlignment="Stretch" ItemsSource="{Binding Fields}" ItemTemplateSelector="{StaticResource MbinTemplateSelector}" >
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListView>
        </ScrollViewer>
    </Grid>
</UserControl>
